spring:
  thymeleaf:
    mode: HTML
  datasource:
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
logging:
  level:
    root: info